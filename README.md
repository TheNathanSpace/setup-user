# 'Setup User'

A centralized collection of my Bash aliases, custom scripts, etc. Basically, everything I need to make a Linux system
feel like home 🥰.

```bash
(apt install git || sudo apt install git) && \
git clone "https://github.com/TheNathanSpace/setup-user"
```

Or:

```bash
wget -qO- setup.thenathan.space | bash
```
